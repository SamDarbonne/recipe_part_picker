<% if current_user && current_user == @user%>
	<%= render 'ownprofile' %>
<% end %>

<% if @user.chef && @user != current_user %>
	<%= render 'chef' %>
<% end %>

<% if ((@user.chef == true) == false) && ((current_user == @user) == false) %>
	<%= redirect_to '/' %>
<% end %>
